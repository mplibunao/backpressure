# Rules reference

Status: placeholder reference for Items 10, 15, and 16.

This file will become the consumer-facing rule and preset reference after the manifest exists. The baseline records only the intended boundary:

- `@mplibunao/oxlint-standards` ships fast AST-shape rules.
- `@effect/language-service` owns type-aware semantic diagnostics.
- Consumers can override each oxlint rule in their own config.


# Notices

`@mplibunao/tsconfig` packages MP's shared TypeScript configuration presets for reuse across projects.

The preset shape is derived from MP's `claude-toolkit` TypeScript configs and intentionally enables `exactOptionalPropertyTypes` for stricter optional-property semantics.
